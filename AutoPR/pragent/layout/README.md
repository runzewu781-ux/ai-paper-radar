# 排版区

负责文章最终视觉呈现，不负责改写事实。

- `render_final.py`：最终 HTML 渲染
- `chart_rebuild.py`：图表重建路由
- `reconstruct.py`：图表结构/数据解析
- `structure_rebuild.py`：结构型 SVG 视觉

Lieflat 本体不在这里，见仓库根目录 `Lieflat-Charts-Skill/`。
